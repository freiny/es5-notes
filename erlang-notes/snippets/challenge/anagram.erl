-module(anagram).
-export([init/0]).

init() ->
	.
	% L = [1,2,3],
	% io:format("~p~n", [L|[1,2,3]] ),
	% io:format("~p~n", anagram({[] ,[a,b,c]})),
	% io:format("~p~n", [anagram([],[a,b,c])]).
%
% anagram({L, R}) ->
% 	[X || X <- L]
