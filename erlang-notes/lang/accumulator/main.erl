-module(main).
-export([init/0]).

% init() ->
% 	perm([a,b,c,d]),
% 	init:stop().
%
% perm(L) -> perm_acc(L, []).
%
% perm_acc([],AccumA,AccumB) {}
% perm_acc(L,AccumA,AccumB) ->
% 	io:format("Ho").
